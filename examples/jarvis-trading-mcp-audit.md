# Security Audit: jarvis-trading-mcp

- **Generated:** 2026-05-10 17:55:34 UTC
- **Tool:** mcp-audit v0.3.0
- **Source:** `/Users/blandry/Documents/Automation/github/jarvis-trading-mcp`
- **Files scanned:** 2 (413 lines)

## Score: 10/10 — low risk

- Started at 10.

## Capabilities (auto-detected)

### What this package CAN do
- ✅ Outbound HTTPS to: trading.landrycmd.com
- ✅ Reads env vars: JARVIS_TRADING_API, JARVIS_TRADING_MAX_CALLS_PER_MIN, JARVIS_TRADING_TOKEN (3 distinct)
- ✅ Writes to stdout (2 sites — expected for stdio MCP)

### What this package does NOT do
- ❌ TLS verification on (no verify=False detected)
- ❌ No subprocess, shell, or os.system detected
- ❌ No eval, exec, compile, or dynamic __import__ detected
- ❌ No filesystem writes detected (no open(..., 'w'/'a'), no os.remove, no shutil writes, no Path.write_text)
- ❌ No filesystem reads detected
- ❌ No inbound network listener (stdio only)
- ❌ No unsafe deserialization (no pickle.load, no yaml.load without SafeLoader, no marshal.loads, no dill/cloudpickle)

## Dependencies

From `pyproject.toml`:

- `requests>=2.28`

## Findings

### Low (1)

#### [MCP011] All 1 dep(s) use loose version pins

- **Rule ID:** `MCP011` (suppress with `# mcp-audit: ignore MCP011` on the offending line)
- **Dimension:** Dependencies
- **Severity:** low
- **Score impact:** −0
- **Detail:** From pyproject.toml: requests>=2.28. For an audited release, consider exact pins (==) so a future transitive update can't change behavior under your release.

## Scoring methodology

Score starts at 10. Each finding deducts a fixed amount based on severity:

- **High** (−2 to −3 each): subprocess with shell=True, dynamic exec, TLS verification disabled, suspicious env var reads
- **Medium** (−1 to −2 each): subprocess without shell, filesystem writes, inbound network listeners, dynamic URL destinations, broad-credential env reads
- **Low** (0 to −1 each): URL constructed via f-string/concat, multiple outbound hosts, loose version pins
- **Info** (no deduction): observed behavior worth noting (e.g., reads from filesystem)

Recommendation thresholds:

- **8–10**: Low-risk for friend distribution. Audit confirms a narrow capability surface.
- **5–7**: Medium-risk. Document the flagged behaviors; install with awareness.
- **0–4**: High-risk. Do not install without manual review of the flagged sites.

---

*Generated by [mcp-audit](https://github.com/bradylandry/mcp-audit) — static-analysis security audit for Python MCP packages. Pure stdlib, no LLM, no network, fully reproducible. Same input → same output.*
